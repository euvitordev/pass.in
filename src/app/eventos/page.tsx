import { Header } from "@/components/header";

export default function EventosPag() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-7xl flex-col items-center gap-6 p-8">
      <Header />
    </main>
  );
}
